export enum EavAttributeTypes {
    NUMBER = 'number',
    STRING = 'string',
    BOOLEAN = 'boolean',
    DATETIME = 'datetime',
    ARRAY = 'array',
    OBJECT = 'object',
}

export enum EavAttributeStatus {
    ENABLED = 'enabled',
    DISABLED = 'disabled',
}
